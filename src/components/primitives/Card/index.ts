export { Card, CardHeader, CardBody, CardFooter } from './Card';

import { Card as CardRoot, CardHeader, CardBody, CardFooter } from './Card';

export const CardNs = {
  Root: CardRoot,
  Header: CardHeader,
  Body: CardBody,
  Footer: CardFooter,
};
