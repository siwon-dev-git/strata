export {
  PopoverRoot,
  PopoverTrigger,
  PopoverContent,
  PopoverClose,
} from './Popover';

import {
  PopoverRoot,
  PopoverTrigger,
  PopoverContent,
  PopoverClose,
} from './Popover';

export const Popover = {
  Root: PopoverRoot,
  Trigger: PopoverTrigger,
  Content: PopoverContent,
  Close: PopoverClose,
};
